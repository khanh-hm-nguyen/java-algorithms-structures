package fundamentals;

/**
 * Transposes a 2D matrix.
 * <p>
 * Transposing converts a matrix of size MxN into a matrix of size NxM,
 * where the element at position [i][j] moves to [j][i].
 * </p>
 *
 * @param matrix The 2D array to transpose.
 * @return A new 2D array representing the transposed matrix.
 */
public class MatrixTransposition {
}
